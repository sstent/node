#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../derby/bin/derby" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../derby/bin/derby" "$@"
  ret=$?
fi
exit $ret
